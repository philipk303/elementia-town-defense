# Elemental particle library source recipe

Generated with the built-in image-generation workflow on 2026-08-13. Each state was generated separately as one centered, isolated, opaque chibi fantasy tower-defense VFX mark with generous padding and no hero, scenery, floor, shadow, text, logo, border, or watermark. Sources requested a flat `#ff00ff` chroma background; outputs that arrived with transparency are preserved directly.

State concepts: `fire` is a tapered flame with embers; `steam` is a joined cream-white vapor curl; `wind` is a pale-cyan crescent air curl; `water` is a blue splash droplet; `snow` is a sturdy six-point snowflake; `smoke` is a joined charcoal puff; `debris` is an angular stone chip with fragments. Palette and crisp outlined cel shading follow the established Earth, Water, and Wind effect recipes.

Packaging contract: crop the visible subject, nearest-neighbor fit with 2px minimum padding into one centered untrimmed 64x64 RGBA frame per state, then pack states in ledger order with 2px transparent gutters.
